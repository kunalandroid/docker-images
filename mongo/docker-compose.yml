version: '3'

services:
  database:
    image: mongo:latest
    ports:
    - 27017:27017

    volumes:
    - ./init-mongo.js:docker-entrypoint-initdb.d/init-mongo.js:ro
    - ./mongo-volume:/data/db

  restart:
    always

volumes:
  mongo-volume: